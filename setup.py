from setuptools import setup, find_packages

setup(
    name='evasion_guidance',
    version='0.1.0',
    description='A package for evasion guidance planning.',
    url='url',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'scipy',
        'matplotlib'
    ],

)